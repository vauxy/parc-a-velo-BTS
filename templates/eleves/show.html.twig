{% extends 'base.html.twig' %}

{% block title %}Eleve{% endblock %}

{% block body %}
    <h1>Eleve</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eleve.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ eleve.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ eleve.prenom }}</td>
            </tr>
            <tr>
                <th>Classe</th>
                <td>{{ eleve.classe }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ eleve.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eleve_index') }}">Retour</a>

    <a href="{{ path('eleve_edit', {'id': eleve.id}) }}">Modifier</a>

    {{ include('eleves/_delete_form.html.twig') }}
{% endblock %}
